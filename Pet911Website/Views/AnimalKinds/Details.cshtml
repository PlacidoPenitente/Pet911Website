@model Pet911Website.ViewModels.AnimalKindViewModel

@{
    ViewBag.Title = "Breeds";
}

<h2>Details</h2>

<div>
    <h4>Breed</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.AnimalKind.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.AnimalKind.Name)
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.AnimalKind.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>

<div>
    @{
        Html.RenderPartial("~/Views/Breeds/Index.cshtml", Model);
    }
</div>
