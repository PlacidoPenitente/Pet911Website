@model Pet911Website.Models.Breed

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Breed</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", "Breeds", new { id = Model.Id }, null) |
    @Html.ActionLink("Back to List", "Details", "AnimalKinds", new { id = Model.AnimalKind.Id }, null)
</p>
